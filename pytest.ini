[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    layer2_nightly: marks tests as Layer 2/3 systemic failure mode tests (overseer-grade)
    layer1_success: marks tests as Layer 1 success condition tests
addopts = -m "not slow"
