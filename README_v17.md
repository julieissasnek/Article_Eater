# Article Eater v17 — Dual-Hierarchy Upgrade (based on v16.1)
Ports: Source-of-truth in contracts/ports.json.

Docs under `docs/v17_src/`; materialized code in `migrations/`, `prompts/`, `templates/`, `static/`, `scripts/`.
