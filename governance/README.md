# Governance Overview (added 2025-11-12)
- Keep list & deprecations: `release.keep.yml`, `deprecations.yml`
- Sacred archive: `docs/archive/` immutable without David's approval
- CI: `.github/workflows/governance.yml` and `ci_v3.yml` (lint/type/test/audit)
- Public surface: enforce via ledger/scripts
- Repro artifacts: zipped release + concatenated TXT with `deconcat.py`